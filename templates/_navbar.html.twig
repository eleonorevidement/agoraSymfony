{# fixed-top #}
<nav class="navbar navbar-expand-lg bg-light mb-5">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img class="logo" src="{{ asset('img/logo.png') }}" height="50">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/">Accueil</a>
                </li>
                <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') %}

                    <a class="nav-link" href="http://localhost:8000/user/">Utilisateurs</a>
                    {% endif %}
                </li>
                <li class="nav-item">

                    <a class="nav-link" href="http://localhost:8000/item/">Catalogue</a>
                </li>
                <li class="nav-item">

                    {% if is_granted('ROLE_USER') %}

                    <a class="nav-link" href="http://localhost:8000/cart">Panier</a>
                    {% endif %}
                </li>
                <li class="nav-item">

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                    <a class="nav-link" href="http://localhost:8000/account">Mon compte</a>
                    {% endif %}
                </li>


            </ul>

            <span class="d-flex gap-2">

                {% if app.user == null %}
                    <a class="btn btn-outline-secondary" role="button" href="/register">S'inscrire</a>
                    <a class="btn btn-secondary" href="/login">Se connecter</a>
                {% else %}
                    <em>{{ app.user.userIdentifier }}</em>
                    <a class="btn btn-outline-secondary" href="{{ path('app_logout') }}">Se déconnecter</a>
                {% endif %}
			</span>
        </div>
    </div>
</nav>